[project]
name = "whatsapp-mcp-server"
version = "0.1.0"
description = "WhatsApp MCP tools for ChatGPT and Codex"
readme = "../README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.12,<2",
    "requests>=2.32.3",
]
